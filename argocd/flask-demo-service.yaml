apiVersion: v1
kind: Service
metadata:
  namespace: flask-demo
  name: flask-demo
spec:
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
  type: NodePort
  selector:
    app.kubernetes.io/name: flask-demo